specVersion: "v1"
serviceId: "semiiiproject"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "semiiiproject-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
